// inject
#[derive(Clone)]
pub struct Context {
    // 1. we need runtime storage to read
    // 2. we need offchain kv storage
    // 3. we need http manager
    // 4. we need timer
}

pub struct Handle {}
